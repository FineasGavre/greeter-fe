## Example of a Vue 3 application interacting with Ethereum Smart Contract with an SDK generated by @dethcrypto/eth-sdk

### How to run
1. Install dependencies
```
yarn (or npm install)
```
2. Run eth-sdk codegen tool
```
yarn eth-sdk (or npx eth-sdk)
```
